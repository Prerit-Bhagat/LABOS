#include<iostream>
using namespace std;
int main()
{
	int at,bt,ct,pid;
	//sort axcoriding to at
	int check[];
	rq=pid[0];
	t=[0];
	bt=q;
	while(!check(bt,n))
	{
		--bt[rq];
		++t;
		for(i->n)
		at[i]==time;
		q,push(pid[i]);
		
		if(bt[i]==0 || time==q)
		{
			if(bt[i]==0 )
			{
				xyz[i]=1;
				ct[i]=time;
			}
			else
			q.push(rq);
		}
		rq=q.front();
		q.pop();
		
		
		for
	}
}